# Changelog

## v0.5.0 (2026-05-28)

### 新增
- **动物森友会主题**：新增第三主题选项，基于 animal-island-ui 组件库全量重构
- **ACNH 首页**：绿色岛屿背景 + 药丸按钮 + 2×2 四宫格导航
- **ACNH 聊天页**：`Card`/`Input`/`Button`/`Select` 替换全部交互组件
- **ACNH 故事模块**：新建/章节/选项/时间线全套 ACNH 组件替换
- **ACNH 记忆模块**：日历羊皮纸渐变背景 + 圆角药丸日期块 + 3D 按压阴影
- **ACNH 设置页**：适配宽屏（720px）+ 主题选择卡片 + 数据清除 Modal
- **ACNH 侧边栏**：暖羊皮纸背景 + `#B7C6E5` 选中色 + `#8a7b66` 默认文字
- **ACNH 写日志**：奶油色底 + 虚线分割 + App Teal 保存按钮 + 16:9 宽屏
- **ACNH 故事设定**：Modal blob 弹窗 + 1000 字默认 + 100% 宽输入框

### 优化
- App Teal (`#82d5bb`) 替换全部操作按钮，白色粗体文字
- 确认弹窗全面改用 ACNH `Modal`（关闭打字机效果 + `primary danger` 取消）
- 聊天切换角色不再自动清空对话（继承上下文）
- 全站滚动条统一为卡其色 `#9a835a`
- 记忆字体统一（移除 ZCOOL KuaiLe，使用 ACNH 全局字体栈）
- Select 下拉定位修正（容器宽度 180→200px）
- 宠物面板：卡其色底框 `#9a835a` + 奶油白文字 + 自定义名称可编辑
- 隐私提醒改为 `#e6f9f6` 薄荷绿无边框气泡

### 暂移除
- 宠物模块临时隐藏（首页/聊天/侧栏全部移除 `PetFloating`）

## v0.4.0 (2026-05-26)

### 新增
- **记忆日志模块**：月历视图（马卡龙配色 + ZCOOL KuaiLe 手绘字体）、心情 emoji 选择、写日志/编辑/删除
- **划重点功能**：选中正文文字标记为重点，自动提取为关键词
- **重要日志标记**：重要日志在 AI 聊天时全部注入上下文，普通日志按关键词匹配
- **隐私提醒**：写日志界面底部固定隐私提示（AI 会读取记忆，勿记录敏感信息）
- **动态 token 预算**：重要日志预算 50 万 tokens，普通日志预算 20 万 tokens

## v0.2.0 (2026-05-24)

### 新增
- **手机端响应式适配**：Sidebar 改为 overlay 弹出层、HomePage 按钮尺寸响应式、消息气泡自适应宽度、宠物面板适配小屏
- **宠物触摸拖拽**：支持 touchstart/touchmove/touchend 事件，手机端可拖动宠物
- **对话管理**：清除当前对话、定向删除单条消息（hover X 按钮）、开启新对话（+按钮）、侧边栏对话列表切换
- **故事自定义设定**：用户角色、叙事视角（第一/第三人称）、每章目标字数、输出格式，创建后可随时修改
- **故事列表侧栏**：左侧面板改为"故事列表 + 章节"双层结构，支持多故事切换和章节折叠
- **故事设定编辑面板**：顶栏新增⚙️设定按钮，弹窗编辑角色/视角/字数/格式

### 修复
- **性格首条消息修复**：新对话第一句时，在 system prompt 和用户消息之间插入角色确认分隔句
- **消息发送 Bug**：修复 `getActiveConversation` 快照过期导致最新消息未发送到 AI
- **侧栏深色条块**：桌面侧栏移除 `glass` 类，消除 blur 造成的视觉拖尾

### 优化
- index.html：添加 theme-color meta 标签、修正 title
- 全局样式：添加移动端触摸优化（tap-highlight、touch-action、overscroll-behavior）
- 用户消息气泡：max-width 改为 `22ch`（约20个中文字），自然断行
- **选项交互优化**：点击 AI 生成的选项后填入输入框（而非直接生成），用户可编辑后再续写
- **System Prompt 增强**：生成时自动注入故事参数（用户角色/视角/字数/格式）

## v0.1.0 (2026-05-22)

### 新增
- 项目初始化：Vite + React 19 + TypeScript
- Tailwind CSS v4 + 磨砂玻璃效果 + 浅色/深色主题
- Zustand 状态管理（settings/chat/story/pet 四模块）
- **API 设置模块**：自定义 Base URL、API Key、模型列表自动拉取、主题切换、数据清除
- **多性格 AI 聊天**：3 个内置角色（傲娇·LUMA/甜心/冷酷）、自定义角色、流式传输、性格切换（继承/清空）
- **故事生成器**：创建故事（标题/背景/主角/冲突）、AI 分章生成、3 选项推进、自定义续写、章节时间线、JSON 导出
- **4 种写作风格**：青春恋爱、现代日常、古风、18+（文风指南注入 system prompt，章节间可切换）
- **宠物系统**：伯恩山犬、可拖拽悬浮、属性自动衰减、喂食/抚摸/洗澡互动、经验/金币/等级系统
- **首页 + 侧边栏**：首页双入口（聊天/故事）、侧边栏模块切换
